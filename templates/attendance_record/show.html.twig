{% extends 'base.html.twig' %}

{% block title %}AttendanceRecord{% endblock %}

{% block body %}
    <h1>AttendanceRecord</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ attendance_record.id }}</td>
            </tr>
            <tr>
                <th>Month</th>
                <td>{{ attendance_record.month }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ attendance_record.year }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_attendance_record_index') }}">back to list</a>

    <a href="{{ path('app_attendance_record_edit', {'id': attendance_record.id}) }}">edit</a>

    {{ include('attendance_record/_delete_form.html.twig') }}
{% endblock %}

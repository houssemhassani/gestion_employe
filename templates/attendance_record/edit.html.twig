{% extends 'template.html.twig' %}

{% block title %}Edit AttendanceRecord{% endblock %}

{% block body %}
    <h1>Edit AttendanceRecord</h1>

    {{ include('attendance_record/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_attendance_record_index') }}">back to list</a>

    {{ include('attendance_record/_delete_form.html.twig') }}
{% endblock %}
